#include <stdio.h>
int main()
{
    int a,b,add;
    printf("Enter a first number:");
    scanf("%d",&a);
    printf("Enter a second number:");
    scanf("%d",&b);
    add=a+b;
    printf("Answer: %d", add);
}